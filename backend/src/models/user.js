// Definir el modelo de User como un objeto estándar
const User = {
    UserID: Number,
    Username: String,
    Email: String,
    CreatedAt: Date,
  };
  
  export default User; // Exportar el modelo